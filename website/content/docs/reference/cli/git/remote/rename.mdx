---
title: jj git remote rename
isCommand: true
---

Rename a Git remote

```shell title="Usage"
jj git remote rename <OLD> <NEW>
```

### Arguments

- `<OLD>` — The name of an existing remote
- `<NEW>` — The desired name for `old`

